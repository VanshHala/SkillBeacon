const { Pool } = require('pg'); const pool = new Pool({ user: process.env.DATABASE_USERNAME || 'postgres', host: process.env.DATABASE_HOST || 'localhost', database: process.env.DATABASE_NAME || 'skillbeacon', password: process.env.DATABASE_PASSWORD || '', port: parseInt(process.env.DATABASE_PORT || '5432') }); pool.query('SELECT source_platform, COUNT(job_url) FROM jobs GROUP BY source_platform', (err, res) => { console.log(err ? err.stack : res.rows); pool.end(); });
